# MariaDB on alpine Docker image

I couldn't find a MariaDB on alpine Docker image so I made my own. It just
installs MariaDB on alpine using the default packages from official alpine apk
repository.
